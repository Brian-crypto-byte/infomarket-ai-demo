{
  "name": "infomarket-ai-demo",
  "version": "0.1.0",
  "private": true,
  "description": "infomarket.ai web demo and mock API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server/config.js && node --check server/api.js"
  },
  "engines": {
    "node": ">=20"
  }
}
